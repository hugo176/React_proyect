import React from 'react'
import { withhWidth, Typography, Hidden } from "@material-ui/core"

//rfce [] {} #

export const Oculto = () => {
    return (
        <div>
            
        </div>
    )
}

export default withWidth()(Oculto)